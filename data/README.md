# Data policy

`fixtures/sample_corpus.jsonl` is a tiny multilingual smoke-test corpus. Production ingestion must use a documented, licensed subset of the target dataset and record its source version, language coverage, passage identifiers, and split.

Do not place downloaded corpora, API credentials, or generated indexes under version control.

